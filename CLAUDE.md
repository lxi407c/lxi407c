# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Purpose

This is a GitHub profile repository (`lxi407c/lxi407c`). The `README.md` at the root is a special file that GitHub renders as the public profile page for the user `lxi407c`. There is no application code, build system, or test suite.

## Structure

The repository contains a single meaningful file:

- `README.md` — GitHub profile page rendered at `https://github.com/lxi407c`. Uses HTML `<img>` tags with shield.io badges and capsule-render for visual styling.

## Conventions

- Badge images use the `shields.io` format: `https://img.shields.io/badge/<label>-<color>.svg?&style=for-the-badge&logo=<logo>&logoColor=<color>`
- The header banner is generated via `capsule-render.vercel.app`
- Sections are separated by `</br>` (non-standard but consistent with the existing style)
- Stack badges are grouped in `<div>` blocks by category (languages, frameworks/libraries, platforms, web)
